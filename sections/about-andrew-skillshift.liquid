{% comment %}
  About Andrew Section - SkillShift Custom
  
  Displays instructor bio with:
  - Professional headshot
  - Personal story and background
  - Key credentials and certifications
  - Achievement stats
  - Social proof elements
  - Call-to-action buttons
{% endcomment %}

{% render 'schema-instructor', name: section.settings.name, job_title: section.settings.title, description: section.settings.bio %}

<section class="about-instructor" id="about-andrew">
  <div class="container">
    {%- if section.settings.heading != blank -%}
      <h2 class="section-title">{{ section.settings.heading }}</h2>
    {%- endif -%}
    
    <div class="about-content">
      <div class="about-image-column">
        {%- if section.settings.image != blank -%}
          <div class="about-image-wrapper">
            {{ section.settings.image | image_url: width: 500 | image_tag: class: 'about-image', loading: 'lazy', alt: section.settings.name }}
          </div>
        {%- endif -%}
        
        {%- if section.blocks.size > 0 -%}
          <div class="about-stats">
            {%- for block in section.blocks -%}
              {%- if block.type == 'stat' -%}
                <div class="about-stat" {{ block.shopify_attributes }}>
                  <div class="about-stat-number">{{ block.settings.number }}</div>
                  <div class="about-stat-label">{{ block.settings.label }}</div>
                </div>
              {%- endif -%}
            {%- endfor -%}
          </div>
        {%- endif -%}
      </div>
      
      <div class="about-text-column">
        {%- if section.settings.name != blank -%}
          <h3 class="about-name">{{ section.settings.name }}</h3>
        {%- endif -%}
        
        {%- if section.settings.title != blank -%}
          <p class="about-title">{{ section.settings.title }}</p>
        {%- endif -%}
        
        {%- if section.settings.bio != blank -%}
          <div class="about-bio">
            {{ section.settings.bio }}
          </div>
        {%- endif -%}
        
        {%- if section.settings.credentials != blank -%}
          <div class="about-credentials">
            <h4 class="credentials-heading">
              <i class="fas fa-certificate"></i>
              Credentials & Expertise
            </h4>
            <div class="credentials-content">
              {{ section.settings.credentials }}
            </div>
          </div>
        {%- endif -%}
        
        {%- if section.blocks.size > 0 -%}
          <div class="about-achievements">
            {%- for block in section.blocks -%}
              {%- if block.type == 'achievement' -%}
                <div class="achievement-badge" {{ block.shopify_attributes }}>
                  <i class="fas fa-{{ block.settings.icon }}"></i>
                  <span>{{ block.settings.text }}</span>
                </div>
              {%- endif -%}
            {%- endfor -%}
          </div>
        {%- endif -%}
        
        {%- if section.settings.primary_cta_text != blank or section.settings.secondary_cta_text != blank -%}
          <div class="about-ctas">
            {%- if section.settings.primary_cta_text != blank -%}
              <a href="{{ section.settings.primary_cta_link }}" class="btn btn-primary">
                {{ section.settings.primary_cta_text }}
              </a>
            {%- endif -%}
            
            {%- if section.settings.secondary_cta_text != blank -%}
              <a href="{{ section.settings.secondary_cta_link }}" class="btn btn-outline">
                {{ section.settings.secondary_cta_text }}
              </a>
            {%- endif -%}
          </div>
        {%- endif -%}
      </div>
    </div>
  </div>
</section>

{% stylesheet %}
  .about-instructor {
    padding: var(--space-xl) 0;
    background: white;
  }

  .about-content {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: var(--space-xl);
    align-items: start;
    margin-top: var(--space-lg);
  }

  .about-image-column {
    position: sticky;
    top: var(--space-lg);
  }

  .about-image-wrapper {
    border-radius: var(--radius-xl);
    overflow: hidden;
    box-shadow: var(--shadow-lg);
    margin-bottom: var(--space-lg);
  }

  .about-image {
    width: 100%;
    height: auto;
    display: block;
  }

  .about-stats {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-md);
  }

  .about-stat {
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    padding: var(--space-lg); /* Increased from var(--space-md) */
    border-radius: var(--radius-lg);
    text-align: center;
    color: white;
    box-shadow: var(--shadow-md); /* Added shadow for depth */
  }

  .about-stat-number {
    font-size: 2rem; /* Increased from 1.75rem */
    font-weight: 700;
    line-height: 1;
    margin-bottom: var(--space-sm); /* Increased from var(--space-xs) */
  }

  .about-stat-label {
    font-size: 0.9375rem; /* Increased from 0.875rem */
    opacity: 0.95;
  }

  .about-text-column {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .about-name {
    font-size: 2.25rem; /* Increased from 2rem */
    font-weight: 700;
    color: var(--color-primary);
    margin: 0;
  }

  .about-title {
    font-size: 1.375rem; /* Increased from 1.25rem */
    color: var(--color-accent);
    font-weight: 600;
    margin: 0;
  }

  .about-bio {
    color: var(--color-body);
    line-height: 1.7;
    font-size: 1.0625rem;
  }

  .about-bio p {
    margin-bottom: var(--space-md);
  }

  .about-bio p:last-child {
    margin-bottom: 0;
  }

  .about-credentials {
    background: var(--color-surface);
    padding: var(--space-lg);
    border-radius: var(--radius-lg);
    border-left: 4px solid var(--color-accent);
  }

  .credentials-heading {
    font-size: 1.25rem; /* Increased from 1.125rem */
    font-weight: 700;
    color: var(--color-primary);
    margin: 0 0 var(--space-sm) 0;
    display: flex;
    align-items: center;
    gap: var(--space-xs);
  }

  .credentials-heading i {
    color: var(--color-accent);
  }

  .credentials-content {
    color: var(--color-body);
    line-height: 1.6;
    font-size: 1.0625rem; /* Added explicit size */
  }

  .credentials-content p {
    margin: 0;
  }

  .about-achievements {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-sm);
  }

  .achievement-badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-xs);
    background: white;
    border: 2px solid var(--color-accent);
    color: var(--color-primary);
    padding: var(--space-sm) var(--space-lg); /* Increased padding */
    border-radius: var(--radius-full);
    font-size: 0.9375rem; /* Increased from 0.875rem */
    font-weight: 600;
    transition: all var(--transition-base);
  }

  .achievement-badge:hover {
    background: var(--color-accent);
    color: var(--navy-deep); /* Changed from white to dark navy for better contrast */
    transform: translateY(-2px);
    box-shadow: var(--shadow-md); /* Added shadow on hover */
  }

  .achievement-badge i {
    color: var(--color-accent);
    transition: color var(--transition-base);
    font-size: 1.125rem; /* Make icons slightly larger */
  }

  .achievement-badge:hover i {
    color: var(--navy-deep); /* Changed from white to dark navy */
  }

  .about-ctas {
    display: flex;
    gap: var(--space-md);
    margin-top: var(--space-md);
  }

  /* Enhanced Mobile Responsiveness */
  @media (max-width: 1023px) {
    .about-content {
      grid-template-columns: 1fr;
      gap: var(--space-lg);
    }

    .about-image-column {
      position: static;
    }

    .about-stats {
      grid-template-columns: repeat(2, 1fr);
    }

    .about-achievements {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 640px) {
    .about-stats {
      grid-template-columns: 1fr;
    }

    .about-achievements {
      grid-template-columns: 1fr;
    }

    .about-ctas {
      flex-direction: column;
    }

    .about-ctas .btn {
      width: 100%;
      justify-content: center;
    }

    .about-name {
      font-size: 1.5rem;
    }

    .about-title {
      font-size: 1.125rem;
    }
  }

    .about-ctas .btn {
      width: 100%;
      text-align: center;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "About Andrew - SkillShift",
  "tag": "section",
  "class": "section-about-instructor",
  "settings": [
    {
      "type": "header",
      "content": "Section Header"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Section Heading",
      "default": "Meet Your Instructor"
    },
    {
      "type": "header",
      "content": "Personal Information"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Professional Headshot"
    },
    {
      "type": "text",
      "id": "name",
      "label": "Full Name",
      "default": "Andrew Gardner"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Professional Title",
      "default": "Technology & Automation Consultant"
    },
    {
      "type": "richtext",
      "id": "bio",
      "label": "Bio / Story",
      "default": "<p>I'm a technology consultant specializing in automation, data systems optimization, and full-stack development. With extensive experience architecting solutions for enterprise clients and growing businesses, I've helped organizations streamline operations, eliminate inefficiencies, and leverage technology to achieve measurable results.</p><p>My approach combines deep technical expertise with a focus on practical, business-driven outcomes. From building responsive web applications with React to engineering data validation systems with Python and SQL, I deliver solutions that solve real problems and create lasting value. My work has spanned industries including transportation, technology consulting, and digital media.</p><p>Through SkillShift, I'm committed to sharing the strategies, frameworks, and technical skills that drive success in today's digital economy. My courses are designed for professionals who want to advance their careers, entrepreneurs building their businesses, and anyone ready to master the tools that power modern technology.</p>"
    },
    {
      "type": "richtext",
      "id": "credentials",
      "label": "Credentials & Expertise",
      "default": "<p><strong>Core Competencies:</strong> Full-Stack Development • Data Systems & SQL • Python Automation • JavaScript & React • API Integration • Process Optimization • Project Management</p><p><strong>Technologies:</strong> Python, JavaScript, React, SQL, Java, Shopify, Drupal, GitHub, Salesforce CRM, Figma, Agile Development</p><p><strong>Professional Experience:</strong> Technology Consultant • Systems Integration Specialist at NJ Transit • Freelance Web Developer & Automation Engineer</p>"
    },
    {
      "type": "header",
      "content": "Call to Action"
    },
    {
      "type": "text",
      "id": "primary_cta_text",
      "label": "Primary Button Text",
      "default": "View All Courses"
    },
    {
      "type": "url",
      "id": "primary_cta_link",
      "label": "Primary Button Link",
      "default": "/collections/all"
    },
    {
      "type": "text",
      "id": "secondary_cta_text",
      "label": "Secondary Button Text",
      "default": "Connect on LinkedIn"
    },
    {
      "type": "url",
      "id": "secondary_cta_link",
      "label": "Secondary Button Link"
    }
  ],
  "blocks": [
    {
      "type": "stat",
      "name": "Stat Badge",
      "settings": [
        {
          "type": "text",
          "id": "number",
          "label": "Number",
          "default": "5+"
        },
        {
          "type": "text",
          "id": "label",
          "label": "Label",
          "default": "Years Experience"
        }
      ]
    },
    {
      "type": "achievement",
      "name": "Achievement Badge",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Icon",
          "default": "check-circle",
          "info": "Font Awesome icon name without 'fa-' prefix"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Badge Text",
          "default": "Dean's List"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "About Andrew - SkillShift",
      "category": "Instructor",
      "blocks": [
        {
          "type": "stat",
          "settings": {
            "number": "5+",
            "label": "Years Experience"
          }
        },
        {
          "type": "stat",
          "settings": {
            "number": "50+",
            "label": "Clients Served"
          }
        },
        {
          "type": "stat",
          "settings": {
            "number": "100+",
            "label": "Projects Delivered"
          }
        },
        {
          "type": "stat",
          "settings": {
            "number": "10+",
            "label": "Technologies"
          }
        },
        {
          "type": "achievement",
          "settings": {
            "icon": "cogs",
            "text": "Automation Specialist"
          }
        },
        {
          "type": "achievement",
          "settings": {
            "icon": "database",
            "text": "Data Systems Expert"
          }
        },
        {
          "type": "achievement",
          "settings": {
            "icon": "code",
            "text": "Full-Stack Developer"
          }
        },
        {
          "type": "achievement",
          "settings": {
            "icon": "chart-line",
            "text": "Process Optimization"
          }
        },
        {
          "type": "achievement",
          "settings": {
            "icon": "project-diagram",
            "text": "Systems Integration"
          }
        },
        {
          "type": "achievement",
          "settings": {
            "icon": "rocket",
            "text": "Enterprise Solutions"
          }
        }
      ]
    }
  ]
}
{% endschema %}
